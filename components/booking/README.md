# Booking components

Customer-facing composition for service selection, barber preference, slots, hold countdown, identity, policy summary, and receipt upload. Components consume safe DTOs and do not calculate availability.
